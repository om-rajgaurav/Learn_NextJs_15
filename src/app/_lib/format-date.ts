// This underscore before a folder name indicates that the folder is a private folder and can only be used internally, not exported outside this file. router will also not read it

// but what if we create a pages.tsx in private folders?
